// Phase 3: same-origin client for POST /api/analyze returning the typed
// success response or the stable error envelope from src/shared/audit-types.
// Intentionally unimplemented in Phase 1.
export {};
