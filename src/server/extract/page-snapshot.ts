// Phase 4: compact PageSnapshot built from static HTML (metadata, heading
// outline, capped text excerpt, limited element samples, counts). No raw
// HTML ever reaches the prompt or the client. Intentionally unimplemented
// in Phase 1.
export {};
