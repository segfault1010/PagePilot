// Canonical sample lives in src/shared so both the API placeholder and the
// client preview render identical contract-valid data; this re-export keeps
// the tests/fixtures convention stable.
export { sampleReport } from "../../src/shared/sample-report";
